const config = {
  theme: {
    extend: {
      dropShadow: {
        white: '0 20px 20px rgba(255, 255, 255, 0.5)', 
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
